kind: Service
apiVersion: v1
metadata:
  labels:
    k8s-app: kubernetes-dashboard
  name: kubernetes-dashboard
  namespace: kubernetes-dashboard
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8443
      nodePort: 32000
  type: NodePort
  selector:
    k8s-app: kubernetes-dashboard
